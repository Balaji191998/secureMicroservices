@model IEnumerable<movies.client.dbentities.TblCustomer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerNmae)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerCity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerOrderedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerNmae)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerCity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerOrderedDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Customerno">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Customerno">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Customerno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
